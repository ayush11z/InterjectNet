# Core audio ML
# torch/torchaudio pinned together and held back: torchaudio>=2.9 removed the
# I/O API (AudioMetaData) that pyannote.audio 3.x still imports at load time.
torch==2.5.1
torchaudio==2.5.1
# huggingface_hub>=1.0 dropped hf_hub_download(use_auth_token=...), which
# pyannote.audio 3.x still calls internally; transformers needs hub>=1.5
# anyway, so we patch pyannote at runtime instead (see src/utils/hf_compat.py).
huggingface_hub>=1.5
pyannote.audio>=3.1
faster-whisper>=1.0
librosa>=0.10
soundfile>=0.12
numpy>=1.26,<2.0
numba>=0.59

# Interjection scoring (Stage 3 uses simple embeddings for topic stability)
sentence-transformers>=2.7

# Demo UI
streamlit>=1.35
matplotlib>=3.8
pandas>=2.0

# Evaluation (weight fitting, public-benchmark eval)
scikit-learn>=1.4
scipy>=1.11
datasets>=2.19

# Utility
python-dotenv>=1.0
pyyaml>=6.0
